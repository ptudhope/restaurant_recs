<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All favorites
    </h1>

    <a href="/favorites/new" class="btn btn-block btn-outline-secondary">
      Add a new favorite
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "favorite_templates/favorites_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Restaurant
        </th>

        <th>
          User
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @favorites.each do |favorite| %>
      <tr>
        <!-- Display favorite.id start -->
        <td>
          <%= favorite.id %>
        </td>
        <!-- Display favorite.id end -->

        <!-- Display favorite.restaurant_id start -->
        <td>
          <% if favorite.restaurant.present? %>
            <a href="/restaurants/<%= favorite.restaurant.id %>">
              <%= favorite.restaurant.name %>
            </a>
          <% end %>
        </td>
        <!-- Display favorite.restaurant_id end -->

        <!-- Display favorite.user_id start -->
        <td>
          <% if favorite.user.present? %>
            <a href="/users/<%= favorite.user.id %>">
              <%= favorite.user.created_at %>
            </a>
          <% end %>
        </td>
        <!-- Display favorite.user_id end -->

        <!-- Display favorite.created_at start -->
        <td>
          <%= time_ago_in_words(favorite.created_at) %> ago
        </td>
        <!-- Display favorite.created_at end -->

        <!-- Display favorite.updated_at start -->
        <td>
          <%= time_ago_in_words(favorite.updated_at) %> ago
        </td>
        <!-- Display favorite.updated_at end -->

        <td>
          <a href="/favorites/<%= favorite.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @favorites, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
